export interface chat_entry {
        role : string,
        avatar ?: string,
        alt ?: string,
        title: string,
        subtitle?: string,
        date ?: string,
        unread ?: boolean
    }